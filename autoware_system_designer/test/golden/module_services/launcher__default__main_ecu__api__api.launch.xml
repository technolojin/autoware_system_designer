<?xml version="1.0"?>
<launch>
  <!-- This launch file is auto-generated by the autoware_system_designer -->
  <!-- Component Launcher: api -->
  <!-- Compute Unit: main_ecu -->
  <!-- This launcher starts all nodes in component: api -->


  <!-- Node: /api/provider -->
  <node pkg="services_pkg" exec="provider_node" name="provider" namespace="/api" output="screen">
    <!-- Remap input and output topics -->
    <remap from="~/output/status" to="/api/status"/>
    <remap from="~/output/set_mode" to="/api/set_mode"/>
    <remap from="~/output/reset" to="/api/reset"/>

    <!-- Parameters - Non-Override Parameters -->

    <!-- Parameters - Overrides -->

    <!-- Parameters - Mode Overrides (highest priority) -->

  </node>

  <!-- Node: /api/consumer -->
  <node pkg="services_pkg" exec="consumer_node" name="consumer" namespace="/api" output="screen">
    <!-- Remap input and output topics -->
    <remap from="~/input/status" to="/api/status"/>
    <remap from="~/input/set_mode" to="/api/set_mode"/>
    <remap from="~/input/reset" to="/api/reset"/>

    <!-- Parameters - Non-Override Parameters -->

    <!-- Parameters - Overrides -->

    <!-- Parameters - Mode Overrides (highest priority) -->

  </node>

</launch>
