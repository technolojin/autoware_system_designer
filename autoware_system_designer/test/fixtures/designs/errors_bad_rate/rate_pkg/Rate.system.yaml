autoware_system_design_format: 0.4.0

name: Rate.system

description: Uses the node with the unresolvable rate.

modes:
  - name: default
    description: Default mode
    default: true

components:
  - name: ticker
    entity: Ticker.node
    compute_unit: main_ecu

connections: []
